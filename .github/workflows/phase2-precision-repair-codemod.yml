name: Phase 2 Precision Repair Codemod

on:
  push:
    branches:
      - phase2/producer-brain-weakness-aware-search
    paths:
      - .github/workflows/phase2-precision-repair-codemod.yml
      - scripts/apply-phase2-precision-repairs.mjs

permissions:
  contents: write

jobs:
  patch-test-benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: phase2/producer-brain-weakness-aware-search
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply precision repair patch
        run: node scripts/apply-phase2-precision-repairs.mjs
      - name: Run focused Producer Brain tests
        run: node --test tests/producer-brain-specialized-repair.test.mjs tests/producer-brain-repair-acceptance.test.mjs tests/producer-brain-surgical-repair.test.mjs tests/music-engine.test.mjs
      - name: Benchmark repair effectiveness
        run: npm run benchmark:repairs -- --seeds 2 --bars 8
      - name: Commit validated precision repairs
        run: |
          if git diff --quiet -- src/music-engine.js tests/producer-brain-specialized-repair.test.mjs; then
            echo "No validated changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/music-engine.js tests/producer-brain-specialized-repair.test.mjs
          git commit -m "Phase 2: add precision producer repairs"
          git push origin HEAD:phase2/producer-brain-weakness-aware-search
